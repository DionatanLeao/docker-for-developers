docker run ubuntu
docker run -it ubuntu
docker ps
docker ps -a | docker container ls -a
docker run nginx
docker stop NAMES(eloquent_lalande)
docker run -d nginx
docker run -d -p 80:80 nginx
docker run -d -p 3000:80 nginx